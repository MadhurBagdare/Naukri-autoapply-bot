# Naukri Auto-Apply Bot Dependencies
# Install with: pip install -r requirements.txt
#
# Tested on Python 3.9.6 (macOS system python3).
# Upper bounds are deliberate: Naukri's DOM changes often enough that an
# unattended `pip install -U` picking up a breaking Selenium release would
# surface as "the bot silently applied to nothing" rather than as an error.

# --- Required by naukri_bot (v2) and the legacy scripts ---
selenium>=4.15.0,<5.0.0
beautifulsoup4>=4.12.0,<5.0.0
lxml>=4.9.0,<6.0.0
python-dotenv>=1.0.0,<2.0.0
webdriver-manager>=4.0.0,<5.0.0

# Facts file (profile.yaml) parsing - naukri_bot only
PyYAML>=6.0,<7.0

# --- Required by the legacy scripts only ---
# Naukri-Edge.py parses with html5lib and writes results with pandas.
# naukri_bot uses lxml and sqlite3 (stdlib) instead.
html5lib>=1.1,<2.0
pandas>=1.3.0,<3.0.0

# --- Optional ---
# The default LLM backend shells out to the `claude` CLI, which needs no API
# key. Install this only if you set ANTHROPIC_API_KEY and LLM_BACKEND=anthropic.
# anthropic>=0.40.0,<1.0.0
